export const ActiveOrders = () => {
    // set state as fetch call that retrieves all active orders
    // create handleFunction to update order and reset order state when order is updates
    
    // STRETCH*** create filter dropdown that allows employee to filter 

    // switch-case:
        // if orderStatus === 1, let update button read "Build Order"
        // if orderStatus === 2, let update button read "Complete Order"
        // if orderStatus === 3, let update button read "Serve Order"

    return(
        <div>
            Active!
        </div>
    )
}